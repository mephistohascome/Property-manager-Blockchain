# PostgreSQL (match docker-compose defaults, or your own instance)
DATABASE_URL=postgresql+psycopg2://compliance:compliance_dev@localhost:5433/cloud_compliance

# Quick local demo without Docker — SQLite (SQLAlchemy URL)
# DATABASE_URL=sqlite:///./data/compliance.db
